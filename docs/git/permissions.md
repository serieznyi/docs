# Не фиксируем изменния прав доступа

Выключаем фиксацию прав доступа (локально)

```bash
git config core.fileMode false
```

Включаем фиксацию прав доступа (локально)

```bash
git config core.fileMode true
```